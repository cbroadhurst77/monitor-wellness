<Window x:Class="MonitorWellness.BlackoutPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview 0% brightness?"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" FontSize="14">
            <Run Text="0% brightness makes your screen completely black. "/>
            <Run Text="For safety, this preview automatically returns to 20% after 15 seconds. Press Escape at any time to restore it immediately."/>
        </TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="RevertButton" Content="Revert now (recommended)" MinWidth="155" Padding="8,4" Margin="0,0,8,0"
                    IsDefault="True" Click="RevertButton_Click"/>
            <Button Content="Preview 0% for 15 seconds" MinWidth="170" Padding="8,4" Click="KeepButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
